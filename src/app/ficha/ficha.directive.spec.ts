import { FichaDirective } from './ficha.directive';

describe('FichaDirective', () => {
  it('should create an instance', () => {
    const directive = new FichaDirective();
    expect(directive).toBeTruthy();
  });
});
